<div class="container-fluid">
  <div class="row post post_margin">
    <div class="col-xs-12 categories_body">
      <div class="upper">
        <div class="buttons">
          <div class="button red">
          </div>
          <div class="button yellow">
          </div>
          <div class="button green">
          </div>
        </div>
      </div>
      <div class="enter">
        <h2>edit_a_post</h3>
          <div>
            <%= form_for(@post) do |f| %>
              <%= render 'shared/error_messages1' %>

              <%= f.label :title %>
              <%= f.text_area :title, class: 'form-control' %>

              <%= f.label :body %>
              <%= f.text_area :body, class: 'form-control body_field' %>

              <%= f.label :category %>
              <%= f.collection_select(:category_id, Category.all, :id, :name, {:prompt => "Please select a category"}) %>

              <div class="text-center buttons-margin-top">
              <%= f.submit "Save Changes", class: 'btn btn-primary' %>
              </div>
            <% end %>
          </div>
      </div>
    </div>
  </div>
</div>
