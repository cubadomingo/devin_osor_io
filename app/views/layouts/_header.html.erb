<nav class="navbar navbar-default navbar-fixed-top navbar-custom">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Devin_Osorio", root_url(subdomain: false), class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="drop_down_nav_font_size nav navbar-nav navbar-right">
        <% if params[:controller] == "static_pages" %>
          <li><%= link_to "[check out my blog]", root_url(subdomain: 'blog'), data: {toggle: "collapse", target: ".navbar-collapse"} %></li>

        <% else %>
          <li><%= link_to "home", root_url(subdomain: false), data: {toggle: "collapse", target: ".navbar-collapse"} %></li>
          <li><%= link_to "[check out my blog]", root_path, data: {toggle: "collapse", target: ".navbar-collapse"} %></li>
          <li><%= link_to "categories", categories_path, data: {toggle: "collapse", target: ".navbar-collapse"} %></li>
        <% end %>

        <% if logged_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle orange" data-toggle="dropdown">account<b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "settings", edit_user_path(current_user, subdomain: 'blog') %></li>
            <li class="divider"></li>
            <li>
            <%= link_to "log_out", logout_path(subdomain: 'blog'), method: "delete" %>
            </li>
          </ul>
        </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
